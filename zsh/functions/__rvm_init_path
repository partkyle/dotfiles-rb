# rvm
[[ -z "$rvm_home" ]] && export rvm_home="$HOME/.rvm"
PATH=$PATH:$rvm_home/bin # Add RVM to PATH for scripting
[[ -s "$rvm_home/scripts/rvm" ]] && source "$rvm_home/scripts/rvm" # Load RVM into a shell session *as a function*

# vim settings
# filetype: zsh
