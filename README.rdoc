= Kyle Partridge Dot Files

These settings were initially forked from @ryanb, and have since diverged considerably.


== Installation

  git clone git://github.com/ryanb/dotfiles ~/.dotfiles
  cd ~/.dotfiles
  git submodule update --init
  rake install
